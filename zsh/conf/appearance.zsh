########################################################################################
### ZSH APPEARANCE
########################################################################################

# This file contains theme and display settings for the shell.
# These must be set before Oh My Zsh is loaded.

########################
### THEME ###
########################
# Set name of the theme to load --- if set to "random", it will load a random theme
# each time Oh My Zsh is loaded. See https://github.com/ohmyzsh/ohmyzsh/wiki/Themes
ZSH_THEME="cloud"

########################
### OH MY ZSH STYLE ###
########################
# Auto-update behavior: reminder | auto | disabled
zstyle ':omz:update' mode reminder

# How often to auto-update (in days)
# zstyle ':omz:update' frequency 13

# Disable colors in ls
# DISABLE_LS_COLORS="true"

# Disable auto-setting terminal title
# DISABLE_AUTO_TITLE="true"

# Display red dots whilst waiting for completion
# COMPLETION_WAITING_DOTS="true"

# Disable marking untracked files dirty (faster for large repos)
# DISABLE_UNTRACKED_FILES_DIRTY="true"

# History command output date format: "mm/dd/yyyy" | "dd.mm.yyyy" | "yyyy-mm-dd"
# HIST_STAMPS="mm/dd/yyyy"
